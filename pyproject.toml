[project]
name = "video-download"
version = "0.1.0"
description = "Telegram bot to download videos from Threads, Instagram, YouTube, VK, and TikTok"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.2",
    "python-telegram-bot>=22.8",
    "yt-dlp[default]>=2026.7.4",
]
